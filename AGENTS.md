# AGENTS.md

## 模型路由

目标：在保证正确性的前提下控制成本。

- **GPT-5.6 Sol**：复杂任务、方向规划、关键决策、高风险问题。
- **GPT-5.6 Luna Max**：方向明确，但执行仍需较强推理。
- **GPT-5.6 Luna High**：简单、重复、批量、规则明确的任务。

## 选择规则

### Sol
优先用于：
- 产品、架构、技术路线、数据模型等规划
- 从 0 到 1 的方案设计
- 需求模糊、冲突、多方案权衡
- 跨模块复杂问题、未知根因 Bug、大型重构
- 安全、权限、数据迁移、生产环境等高风险操作
- 关键发布或重要方案复核

原则：**错误方向会造成大量返工时，用 Sol。**

### Luna Max
用于：
- 已有明确方案后的功能开发
- 多文件修改、中等重构、复杂业务逻辑
- 已知范围内的 Bug 定位
- API 适配、测试、局部性能优化
- 需要较强上下文理解，但无需重新决定方向

### Luna High
用于：
- 简单代码、配置、样式、文案修改
- 字段、路径、命名调整
- 批量替换、生成、格式整理
- 总结、提取、README、Changelog
- 已有成熟实现可直接模仿的任务

## 自动升降级

```text
Luna High
→ 推理增加 / 范围扩大
→ Luna Max

Luna Max
→ 方案可能错误 / 涉及架构、安全、数据风险
→ Sol
```

低成本模型多次失败时应升级。

Sol 完成需求分析、架构设计、技术决策和任务拆分后，后续执行应尽量降级：

```text
复杂执行 → Luna Max
简单/重复/批量 → Luna High
关键复核 → Sol
```

## 判断标准

不要按代码量或文件数选模型，应看：

**复杂度 + 不确定性 + 重要性 + 风险 + 是否涉及方向决策**

默认：

```text
简单 / 重复 / 批量 → Luna High
普通开发 / 复杂执行 → Luna Max
复杂问题 / 方向规划 / 高风险 → Sol
```

> **Sol 负责想方向、解难题、做关键判断；Luna Max 负责复杂执行；Luna High 负责简单、重复和批量工作。**
