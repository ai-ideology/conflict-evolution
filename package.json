{
  "name": "conflict-evolution",
  "version": "0.1.0",
  "description": "冲突的进化 —— 关于鹰鸽博弈的互动游戏",
  "type": "module",
  "scripts": {
    "dev": "bun run sync:css && bun run --hot serve.ts",
    "start": "bun run sync:css && bun serve.ts",
    "build": "bun run sync:css && bun build ./src/main.ts --outdir ./public/build --target browser --minify",
    "check": "bunx tsc --noEmit",
    "test": "bun test",
    "sync:css": "bun -e \"await Bun.write('public/sketch.css', Bun.file('src/styles/sketch.css'))\""
  },
  "devDependencies": {
    "@types/web": "^0.0.166",
    "bun-types": "^1.2.0",
    "typescript": "^5.6.0"
  }
}
